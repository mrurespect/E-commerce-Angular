import { DollarToDhPipe } from './dollar-to-dh.pipe';

describe('DollarToDhPipe', () => {
  it('create an instance', () => {
    const pipe = new DollarToDhPipe();
    expect(pipe).toBeTruthy();
  });
});
